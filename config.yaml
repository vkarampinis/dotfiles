- defaults:
    link:
      relink: true
      create: true
      glob: true
    shell:
        stdout: true
        stderr: true

- clean: ["~"]

- link:
    ~/.aliases: config/.aliases
    ~/.bash_profile: config/.bash_profile
    ~/.gitconfig: config/.gitconfig

- shell:
    - scripts/homebrew.sh
    - scripts/tools.sh
